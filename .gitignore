# build output
dist/
# generated types
.astro/
# local cache from `npx wrangler pages dev dist`, if you use it to check the
# headers in public/_headers — `npm run preview` does not read that file
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# AI coding-tool config — kept local, deliberately never tracked. These hold
# working notes and machine-specific paths, so they are not part of what this
# template ships.
CLAUDE.md
.claude/
